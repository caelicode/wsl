# Build outputs
images/
*.tar
*.tar.gz

# Node (semantic-release)
node_modules/
# package-lock.json is COMMITTED: the release job runs `npm ci` so the
# toolchain that cuts releases is pinned, not resolved fresh each run.

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Python
__pycache__/
*.pyc
