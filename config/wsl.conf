[automount]
enabled = true
options = "metadata"
mountFsTab = false

[boot]
# systemd powers the daily update-check timer (caelicode-update-check.timer)
systemd = true

[user]
default = caelicode

[interop]
enabled = true
appendWindowsPath = false
